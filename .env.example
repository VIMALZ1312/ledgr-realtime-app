# Copy this file to .env and fill in real values. Do NOT commit .env.
# Real secrets live in Replit Deployment secrets (and Netlify env), not here.

PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret
PLAID_ENV=sandbox
GITHUB_TOKEN=your_github_pat
GITHUB_REPO=VIMALZ1312/ledgr-realtime-app
# Bank tokens added after linking (via /api/exchange-token):
# PLAID_TOKEN_BOFA=access-sandbox-xxx
# PLAID_TOKEN_WF=access-sandbox-xxx
# PLAID_TOKEN_TD=access-sandbox-xxx
# PLAID_TOKEN_DISCOVER=access-sandbox-xxx
